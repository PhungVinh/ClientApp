import { CheckDuplicateAsyncNameDirective } from './check-duplicate-async-name.directive';

describe('CheckDuplicateAsyncNameDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckDuplicateAsyncNameDirective();
    expect(directive).toBeTruthy();
  });
});
